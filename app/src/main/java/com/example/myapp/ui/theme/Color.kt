package com.example.myapp.ui.theme

import androidx.compose.ui.graphics.Color


//================= Colores de la app =================
// Fondo principal de toda la aplicación
val Background = Color(0xFF0B1220)

// Fondo para header y footer
val Surface = Color(0xFF111C2E)

// Fondo para tarjetas o secciones dentro del contenido principal
val CardColor = Color(0xFF17243A)

// Color principal para botones, elementos seleccionados o destacados
val Primary = Color(0xFF3B82F6)

// Color secundario para pequeños detalles o acentos visuales
val Accent = Color(0xFF22D3EE)

// Color para indicar que una moneda subió de valor
val Positive = Color(0xFF22C55E)

// Color para indicar que una moneda bajó de valor
val Negative = Color(0xFFEF4444)

// Color para textos principales, títulos y valores importantes
val TextPrimary = Color(0xFFF8FAFC)

// Color para textos secundarios, descripciones o información menos importante
val TextSecondary = Color(0xFF94A3B8)

// Color para líneas divisorias, bordes o separadores
val DividerColor = Color(0xFF25344D)
